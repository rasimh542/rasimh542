name: Hello World

on:
  push:
    branches:
    - main
  pull_request:
    branches:
      -main
  workflow_dispatch:
jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Hellow World
        run: echo "Hello World!"
        shell: bash
  goodbye:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Goodbye World
        run: echo "Goodbye World!"
        shell: bash
      
        
    
  
      
    
    
